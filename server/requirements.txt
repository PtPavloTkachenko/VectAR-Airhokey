# Use a Python 3.12 venv. Before running set:
#   export PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
# wirepod-vector-sdk = anki_vector fork with py3.12 fixes built in
# (events.py loop arg, annotate.py collections.abc). Pulls grpcio, requests,
# cryptography, zeroconf as its own dependencies.
wirepod-vector-sdk
websockets>=12
protobuf==4.25.8
aiohttp>=3.9
zeroconf
requests
pytest>=8
# The BLE onboarding path: bleak drives the radio (onboarding/ble/transport.py)
# and PyNaCl is the libsodium binding the RTS handshake encrypts with
# (onboarding/ble/crypto.py). Both were missing here until someone set the
# project up on a clean machine and CONNECT VECTOR raised ModuleNotFoundError
# on the first press — they are not optional extras, they are pairing itself.
bleak
pynacl
